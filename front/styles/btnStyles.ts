const styled = require('styled-components').default;


export const btnStyle = (component: JSX.Element) => {
    return styled(component)`
        color: palevioletred;
        font-weight: bold;
    `;
};

